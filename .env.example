# General
APP_NAME="Notification Service"
ENVIRONMENT=dev

# Retry / timeouts
PROVIDER_RETRY_ATTEMPTS=3
PROVIDER_RETRY_INITIAL_DELAY_MS=200
PROVIDER_RETRY_BACKOFF_MULTIPLIER=2.0

# Email settings
EMAIL_ENABLED=true
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL="no-reply@example.com"

# SMS settings
SMS_ENABLED=true
SMS_API_KEY=
SMS_SENDER_ID=

# Telegram settings
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=